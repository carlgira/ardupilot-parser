# ardupilot-parser
ardupilot-parser

# Configuration

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

# Test
Open jupyter lab
```bash
jupyter-lab
```

Open tlog notebook to test the parser